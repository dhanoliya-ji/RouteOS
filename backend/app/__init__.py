"""RouteOS backend application package.

Layered: api -> services -> models/db, with core/ available to all of them and
imports only ever pointing downward. See README.md in this directory for the
full dependency matrix.
"""
